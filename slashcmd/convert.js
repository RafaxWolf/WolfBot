const { SlashCommandBuilder } = require("discord.js") //Import the necessaries library's from discord API

module.exports = {
    data: new SlashCommandBuilder()
    .setName("convert") //Slash command
    .setDescription("asd"), //Somthing

    async run(client, interaction){
      
    }
        
}
